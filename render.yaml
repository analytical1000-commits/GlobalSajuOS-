파일명: render.yaml

내용:
services:
  - type: web
    name: globalsajuos
    runtime: python
    pythonVersion: "3.11"
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn globalsajuos_v13_main:app --host 0.0.0.0 --port $PORT
